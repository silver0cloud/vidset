# ===================================================================
# Open TTS Studio — Backend Requirements
# ===================================================================
# Install: pip install -r requirements.txt --break-system-packages
#
# NOTE: pyaudio requires system portaudio library:
#   Linux:   sudo apt install portaudio19-dev
#   macOS:   brew install portaudio
#   Windows: works out of the box via pip wheel
# ===================================================================

# --- Web framework -------------------------------------------------
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2
websockets==13.1

# --- Audio I/O & processing -----------------------------------------
pyaudio==0.2.14
soundfile==0.12.1
librosa==0.10.2
scipy==1.14.1
numpy==1.26.4

# --- Voice embedding -------------------------------------------------
resemblyzer==0.1.4

# --- ML core (training + inference) ----------------------------------
torch==2.4.1
torchaudio==2.4.1
transformers==4.45.2
accelerate==0.34.2
peft==0.13.0

# --- Inference optimization -------------------------------------------
onnx==1.16.2
onnxruntime==1.19.2
# onnxruntime-gpu==1.19.2   # uncomment instead of onnxruntime if using CUDA inference

# --- F5-TTS base architecture ------------------------------------------
# f5-tts                    # install from source / specific fork once locked, see docs/MODEL_SOURCE.md

# --- Utilities -----------------------------------------------------
httpx==0.27.2               # used by FastAPI TestClient in tests
